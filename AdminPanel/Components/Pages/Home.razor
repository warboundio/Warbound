@page "/"
@inject NavigationManager Navigation

@code {
    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
        Navigation.NavigateTo("/admin/duca", replace: true);
    }
}